---
rails:
  build: .
  ports: 
    - "80:80"
  dns: localhost
  volumes:
    - app:/usr/src/app/app
    - config:/usr/src/app/config
    - db:/usr/src/app/db
    - log:/usr/src/app/log
    - script:/usr/src/app/script